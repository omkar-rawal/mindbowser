import { CapitalizenamePipe } from './capitalizename.pipe';

describe('CapitalizenamePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizenamePipe();
    expect(pipe).toBeTruthy();
  });
});
